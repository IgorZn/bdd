[behave]
color = True
stdout_capture = no
format = json.pretty
outfiles = results.json
log_capture: True

[behave.userdata]
browser = chrome

[behave.formatters]
allure   = allure_behave.formatter:AllureFormatter